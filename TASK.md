# Worktree Task Entry

This worktree runs the **API + Spec** mission.

## What to do

1. Read and execute:
   - `api/TASK.md`
   - `spec/TASK.md`
2. Make changes only under:
   - `api/`
   - `spec/`
3. When finished, fill in:
   - `api/HANDOFF.md`
   - `spec/HANDOFF.md`

## Handoff rule

Do not ask for review until both module handoffs are filled and the branch is clean or has committed work ready for review.
