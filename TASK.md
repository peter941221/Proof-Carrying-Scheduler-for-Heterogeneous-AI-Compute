# Worktree Task Entry

This worktree runs the **Verifier + Proofs** mission.

## What to do

1. Read and execute:
   - `verifier/TASK.md`
   - `proofs/TASK.md`
2. Make changes only under:
   - `verifier/`
   - `proofs/`
3. When finished, fill in:
   - `verifier/HANDOFF.md`
   - `proofs/HANDOFF.md`
